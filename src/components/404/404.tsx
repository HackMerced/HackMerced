import React, { FC } from 'react';

import './404.scss';

const Error: FC = (): JSX.Element => {
    return (
        <>
            <main>
                <div className="error"></div>
            </main>
        </>
    );
};

export default Error;
