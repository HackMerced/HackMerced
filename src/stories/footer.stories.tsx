import { storiesOf } from "@storybook/react";
import * as React from "react";

import Footer from "../components/Footer";

storiesOf("Footer", module).add("Base Footer", () => {
    return <Footer />;
});
