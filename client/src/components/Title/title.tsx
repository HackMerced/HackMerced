import React from 'react';
import './title.scss';

const Title: React.FC = () => {
    return (
        <section className="TITLE">
            <div className="hackmerced-logo"></div>
        </section>
    );
};

export default Title;
